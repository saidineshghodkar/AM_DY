# Copy this file to `.env` on the server and edit the values.
# NEVER commit the real `.env` to source control.

FLASK_ENV=production
SECRET_KEY=replace-with-output-of--openssl-rand-hex-32

# Admin credentials (used by /admin login)
ADMIN_USERNAME=nagesh@1024
ADMIN_PASSWORD=Q2!f<?P